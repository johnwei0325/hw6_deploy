{
  "name": "hw6",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn server",
    "install:prod": "cd frontend && yarn install --freeze-lockfile && yarn add @mui/material @emotion/react @emotion/styled && cd ../backend && yarn install --freeze-lockfile",
    "install:all":"cd frontend && yarn install && cd ../backend && yarn install",
    "build": "cd frontend && yarn build",
    "deploy": "cd backend && yarn deploy"
    
  },
  "dependencies": {
    "@emotion/react": "^11.10.5",
    "@emotion/styled": "^11.10.5",
    "@mui/material": "^5.10.12"
  }
}
